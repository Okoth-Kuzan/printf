#include "main.h"

/**
 * _print_percent - function to print percent
 * @para: percent to print
 *
 * Return: no. of chars
 */

int _print_percent(va_list para)
{
	char t = '%';
	write(1, &t, 1);

	return (1);
}
